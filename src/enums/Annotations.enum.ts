export enum Annotations {
  OPEN = 'Open',
  CLOSE = 'Close',
  WHEEL_SPINNER_ON = 'Wheel spinner on',
  WHEEL_SPINNER_OFF = 'Wheel spinner off',
  EXTEND_HANDLE = 'Extend handle',
  RETRACT_HANDLE = 'Retract handle',
}
